// Legacy API file no longer used.
// Left intentionally minimal so the new hexagonal structure under
// `src/domain`, `src/application`, and `src/infrastructure` is the
// only active public surface.
