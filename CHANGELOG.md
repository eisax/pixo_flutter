## 0.1.0

- Initial Flutter plugin wrapping the Rust `pixo` encoder via Dart FFI.
- Public API: `PixoService`, `PixoPngOptions`, `PixoJpegOptions`.
- Includes unit tests and an integration test exercising `test/test.png`.
